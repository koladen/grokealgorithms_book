Логарифм

log2 32 = 5
Читается как логарифм по основанию 2 числа 32. Означает, в какую степень нужно возвести 2, чтобы получить 32.

Бинарный поиск

Это поиск в ОТСОРТИРОВАННОЙ последовательности. Откидываем половину чисел, потом снова половину и тд. В худшем случае
O(log n)
Если алгоритм выполняется за линейное время - O(n) (например последовательный перебор)
О(п * log п). Пример: эффективные алгоритмы сортировки (быстрая сортировка)
О(п2). Пример: медленные алгоритмы сортировки (сортировка выбором)
О(п!). Пример: очень медленные алгоритмы (задача о коммивояжере)

def binary_search(sequence, item):
    low = 0
    high = len(sequence) - 1
    while low <= high:
        mid = int((low + high) / 2)
        guess = sequence[mid]
        if guess == item:
            return mid
        elif guess > item:
            high = mid - 1
        else:
            low = mid + 1
    return None

